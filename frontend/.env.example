# API Configuration
VITE_API_URL=http://localhost:3001/api

# Demo data mode - Set to "true" to use demo data, "false" to use real API
# Defaults to false if not set
VITE_USE_DEMO_DATA=false

# Google Books API key (optional - works without key but with lower rate limits)
# Get a free key at: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_BOOKS_API_KEY=
