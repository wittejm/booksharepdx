// Feature flags for enabling/disabling functionality

// Email verification flow - when false, users are auto-verified on signup
// and the "Email Verified!" UI is hidden from the location selection page
export const EMAIL_VERIFICATION_ENABLED = false;
